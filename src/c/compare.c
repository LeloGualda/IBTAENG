lude <stdio.h> #include <stdlib.h> /* Dada a sequência s[1..k] gera a próxima em ordem lexicográfica.  Retorna o comprimento da sequência (k-1 ou k+1).  Retorna 0 se chegou ao fim. */  int prox(int s[],int k, int n) { /* caso particular - o primeiro elemento */ if (k==0) { s[1] = 1; return 1; }  /* caso particular - o último elemento */ if (s[1] == n) return 0; if (s[k] < n) { s[k+1] = s[k]+1; return k+1; } s[k-1]++; return k-1; } /* imprime a sequência s[1..k] */ void imprima(int s[], int k) { int i; printf("\n"); for (i=1; i<=k; i++) printf("%4d", s[i]); } /* imprime todas as subsequências de 1..n */ int main() { int * s; int n, k; int cc=1; printf("\nentre com n:"); scanf("%d",&n); s = malloc((n+1)*sizeof(int)); k=0; while (1) { k = prox(s, k, n); if (k == 0) break;  imprima(s,k);   cc++; } printf("\n\n***%5d elementos\n\n", cc); }
