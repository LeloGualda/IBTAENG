import PrimeiroGrau from '../component/criarFuncao';

describe('PrimeiroGrau', () => {
  test('f(10)=10;f(20)=300', () => {
    const f =  PrimeiroGrau(0.1,0.6,0.5,0);

  //  expect(f(10)).toBe(10);
//    expect(f(20)).toBe(300);
  });
});
